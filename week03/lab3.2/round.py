#This program takes in a float and outputs an
#int (rounded up or down)

numberToround = float(input("Enter a float number:")

roundedNumber = round(numberToround)

print('{} rounded is {}').format(x, roundedNumber))